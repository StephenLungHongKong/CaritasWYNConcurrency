@model IEnumerable<CaritasWYN.Models.Act_type>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table ">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ActivityName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ActivityName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Act_typeId" class="btn btn-secondary">Edit</a>
                @*<a asp-action="Details" asp-route-id="@item.Act_typeId" class ="btn btn-primary">Details</a> *@
                <a asp-action="Delete" asp-route-id="@item.Act_typeId" class="btn btn-danger">X</a>
            </td>
        </tr>
}
    </tbody>
</table>
